/**
 * RTBXArchitecture — reusable architecture visualisation component family.
 *
 * Modes:
 *   compact    — small inline badge/label row of the five engine steps
 *   five-step  — visual five-step engine with Travel examples
 *   six-layer  — the six intelligence layers stacked with descriptions
 *   hierarchy  — the five-level Core → Travel configuration → Travel operating systems → Property configuration → Active deployment hierarchy
 *
 * All labels and descriptions are sourced from src/data/rtbxArchitecture.ts.
 * No design-system imports needed — uses the same inline-style tokens as the rest of the app.
 */

export { RTBXArchitecture } from "./RTBXArchitecture";
export type { RTBXArchitectureMode, RTBXArchitectureProps } from "./RTBXArchitecture";
